{
  "name": "@gemma/ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Shared cross-app UI primitives. Today: PageMask (full-page loading mask, used by both apps' route shells) and Spinner (inline border-trick spinner for buttons mid-mutation). Add new chrome here only when the same component would otherwise be copy-pasted between apps/browser and apps/curation.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "peerDependencies": {
    "react": "^18.3.0"
  }
}
