/** Types for the plain-JS mark-component emitter — same reason as
 *  emit-favicon.d.mts beside it. */
export declare const COMPONENT_PATH: string;
export declare function buildMarkComponent(markSvg: string): string;
