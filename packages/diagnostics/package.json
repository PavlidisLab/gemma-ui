{
  "name": "@gemma/diagnostics",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Shared diagnostics primitives — Sample correlation / PCA scree / PC × factor / Mean-variance chart bodies, plus the PanelCard chrome and gene-rows table. Consumed by apps/browser (read-only Diagnostics row on the public dataset page) and apps/curation (curator Diagnostics tab, with extra affordances like outlier marking layered on top via wrapper-level callbacks).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@gemma/heatmap": "*"
  },
  "peerDependencies": {
    "react": "^18.3.0"
  }
}
