.env
calibration-packages/*
!calibration-packages/.gitkeep
# Versioned backup snapshots of calibration packages — large, local-only.
calibration-packages.bak.*/
# package.sh output — large (>100MB) handoff tarball, not for git
curator-handoff-*.tar.gz
